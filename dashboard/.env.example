# Copy to .env in the repo root (same folder as pipeline/, dashboard/) or export before Streamlit.
# Replace USER, PASSWORD, and DBNAME with real values. Host must be a real name (e.g. localhost), not "host".
DATABASE_URL=postgresql+psycopg2://USER:PASSWORD@localhost:5432/DBNAME

# Optional
# STREAMLIT_SERVER_HEADLESS=true
