from dashboard import data # Import the main function to run the dashboard

# Initialize variables to avoid errors
option = option
data = data

# Example checks for data and options
if data is None:
    print("Data is empty.")
else:
    print(f"Data is not empty: {data}")

if option is None:
    print("Option is empty.")
else:
    print(f"Option is not empty: {option}")

# Run the Streamlit dashboard if the script is executed directly
#if __name__ == '__main__':
#   dashboard_main()
